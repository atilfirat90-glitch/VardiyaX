<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ShiftCraft.Mobile.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:ShiftCraft.Mobile.Views"
    Shell.FlyoutBehavior="Flyout"
    FlyoutBackgroundColor="{StaticResource Primary}">

    <Shell.FlyoutHeader>
        <VerticalStackLayout Padding="20" BackgroundColor="{StaticResource Primary}">
            <Frame BackgroundColor="White" 
                   CornerRadius="40" 
                   HeightRequest="80" 
                   WidthRequest="80"
                   HorizontalOptions="Center"
                   Padding="0"
                   HasShadow="False">
                <Label Text="VX" 
                       FontSize="32" 
                       FontAttributes="Bold"
                       TextColor="{StaticResource Primary}"
                       HorizontalOptions="Center"
                       VerticalOptions="Center"/>
            </Frame>
            <Label x:Name="UserNameLabel"
                   Text="VardiyaX" 
                   FontSize="20" 
                   FontAttributes="Bold"
                   TextColor="White"
                   HorizontalOptions="Center"
                   Margin="0,10,0,0"/>
            <Label x:Name="UserRoleLabel"
                   Text="Vardiya Yönetim Sistemi" 
                   FontSize="12"
                   TextColor="White"
                   Opacity="0.8"
                   HorizontalOptions="Center"/>
        </VerticalStackLayout>
    </Shell.FlyoutHeader>

    <!-- Login Route (No Flyout) - DISABLED FOR TESTING -->
    <!--
    <ShellContent
        Route="login"
        ContentTemplate="{DataTemplate views:LoginPage}"
        Shell.FlyoutBehavior="Disabled"
        Shell.NavBarIsVisible="False"
        FlyoutItemIsVisible="False"/>
    -->

    <!-- v1.5: Dashboard (FIRST item) -->
    <FlyoutItem x:Name="DashboardItem" Title="Ana Panel" Icon="dotnet_bot.png" Route="dashboard">
        <ShellContent
            Title="Ana Panel"
            Icon="dotnet_bot.png"
            ContentTemplate="{DataTemplate views:DashboardPage}"/>
    </FlyoutItem>

    <!-- Main TabBar -->
    <FlyoutItem Title="Ana Sayfa" Icon="dotnet_bot.png" Route="main">
        <Tab Title="Çalışanlar">
            <ShellContent
                Title="Çalışanlar"
                Icon="dotnet_bot.png"
                ContentTemplate="{DataTemplate views:EmployeesPage}"/>
        </Tab>
        <Tab Title="Vardiyalar">
            <ShellContent
                Title="Vardiyalar"
                Icon="dotnet_bot.png"
                ContentTemplate="{DataTemplate views:SchedulesPage}"/>
        </Tab>
        <Tab Title="İhlaller">
            <ShellContent
                Title="İhlaller"
                Icon="dotnet_bot.png"
                ContentTemplate="{DataTemplate views:ViolationsPage}"/>
        </Tab>
    </FlyoutItem>

    <!-- Admin Menu -->
    <FlyoutItem x:Name="UserManagementItem" Title="Kullanıcı Yönetimi" Icon="dotnet_bot.png" Route="users">
        <ShellContent
            Title="Kullanıcılar"
            Icon="dotnet_bot.png"
            ContentTemplate="{DataTemplate views:UsersPage}"/>
    </FlyoutItem>

    <!-- Audit Logs Menu -->
    <FlyoutItem x:Name="AuditLogsItem" Title="Denetim Günlükleri" Icon="dotnet_bot.png" Route="auditlogs">
        <ShellContent
            Title="Denetim Günlükleri"
            Icon="dotnet_bot.png"
            ContentTemplate="{DataTemplate views:AuditLogsPage}"/>
    </FlyoutItem>

    <!-- v1.2: Employee Management (Manager/Admin only) -->
    <FlyoutItem x:Name="EmployeeManageItem" Title="Çalışan Yönetimi" Icon="dotnet_bot.png" Route="employeemanage">
        <ShellContent
            Title="Çalışan Yönetimi"
            Icon="dotnet_bot.png"
            ContentTemplate="{DataTemplate views:EmployeeManagePage}"/>
    </FlyoutItem>

    <!-- v1.4: Shift Create (Manager/Admin only) -->
    <FlyoutItem x:Name="ShiftCreateItem" Title="Vardiya Oluştur" Icon="dotnet_bot.png" Route="shiftcreate">
        <ShellContent
            Title="Vardiya Oluştur"
            Icon="dotnet_bot.png"
            ContentTemplate="{DataTemplate views:ShiftCreatePage}"/>
    </FlyoutItem>

    <!-- v1.4: Schedule View (All roles) -->
    <FlyoutItem x:Name="ScheduleViewItem" Title="Program Görüntüle" Icon="dotnet_bot.png" Route="scheduleview">
        <ShellContent
            Title="Program Görüntüle"
            Icon="dotnet_bot.png"
            ContentTemplate="{DataTemplate views:ScheduleViewPage}"/>
    </FlyoutItem>

    <!-- v1.3: Notification Center -->
    <FlyoutItem x:Name="NotificationsItem" Title="Bildirimler" Icon="dotnet_bot.png" Route="notificationcenter">
        <ShellContent
            Title="Bildirimler"
            Icon="dotnet_bot.png"
            ContentTemplate="{DataTemplate views:NotificationsPage}"/>
    </FlyoutItem>

    <!-- Settings Menu -->
    <FlyoutItem x:Name="NotificationSettingsItem" Title="Bildirim Ayarları" Icon="dotnet_bot.png" Route="notifications">
        <ShellContent
            Title="Bildirim Ayarları"
            Icon="dotnet_bot.png"
            ContentTemplate="{DataTemplate views:NotificationPreferencesPage}"/>
    </FlyoutItem>

    <!-- v1.5: Shift Swap -->
    <FlyoutItem x:Name="ShiftSwapItem" Title="Vardiya Takas" Icon="dotnet_bot.png" Route="shiftswap">
        <ShellContent
            Title="Vardiya Takas"
            Icon="dotnet_bot.png"
            ContentTemplate="{DataTemplate views:ShiftSwapPage}"/>
    </FlyoutItem>

    <!-- v1.5: Time Off -->
    <FlyoutItem x:Name="TimeOffItem" Title="İzin Talepleri" Icon="dotnet_bot.png" Route="timeoff">
        <ShellContent
            Title="İzin Talepleri"
            Icon="dotnet_bot.png"
            ContentTemplate="{DataTemplate views:TimeOffPage}"/>
    </FlyoutItem>

    <!-- v1.5: Team Chat -->
    <FlyoutItem x:Name="TeamChatItem" Title="Takım Sohbeti" Icon="dotnet_bot.png" Route="teamchat">
        <ShellContent
            Title="Takım Sohbeti"
            Icon="dotnet_bot.png"
            ContentTemplate="{DataTemplate views:TeamChatPage}"/>
    </FlyoutItem>

    <!-- Logout Menu Item -->
    <MenuItem Text="Çıkış Yap" 
              IconImageSource="dotnet_bot.png"
              Clicked="OnLogoutClicked"/>

</Shell>
