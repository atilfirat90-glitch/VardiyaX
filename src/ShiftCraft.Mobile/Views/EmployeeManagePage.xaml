<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ShiftCraft.Mobile.ViewModels"
             xmlns:models="clr-namespace:ShiftCraft.Mobile.Models"
             x:Class="ShiftCraft.Mobile.Views.EmployeeManagePage"
             x:DataType="vm:EmployeeManageViewModel"
             Title="Çalışan Yönetimi"
             BackgroundColor="{StaticResource Gray100}">
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="+" Command="{Binding AddEmployeeCommand}" />
    </ContentPage.ToolbarItems>
    
    <Grid RowDefinitions="Auto,*">
        
        <!-- Access Denied Message -->
        <Frame Grid.Row="0" 
               IsVisible="{Binding ShowAccessDenied}"
               BackgroundColor="#FFEBEE"
               BorderColor="#EF5350"
               Margin="10"
               Padding="15"
               CornerRadius="8">
            <Label Text="Bu sayfaya erişim yetkiniz yok. Sadece Yöneticiler erişebilir."
                   TextColor="#C62828"
                   HorizontalTextAlignment="Center" />
        </Frame>
        
        <!-- Employee List -->
        <RefreshView Grid.Row="1"
                     IsRefreshing="{Binding IsBusy}" 
                     Command="{Binding RefreshCommand}"
                     RefreshColor="{StaticResource Primary}"
                     IsVisible="{Binding ShowAccessDenied, Converter={StaticResource InverseBoolConverter}}">
            
            <CollectionView ItemsSource="{Binding Employees}"
                            SelectionMode="None">
                
                <CollectionView.Header>
                    <StackLayout Padding="15,10">
                        <Label Text="{Binding Employees.Count, StringFormat='{0} çalışan'}"
                               FontSize="12"
                               TextColor="{StaticResource Gray500}"/>
                    </StackLayout>
                </CollectionView.Header>
                
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Employee">
                        <Frame Margin="10,5" Padding="15" CornerRadius="10" HasShadow="True" BackgroundColor="White">
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer 
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:EmployeeManageViewModel}}, Path=EditEmployeeCommand}"
                                    CommandParameter="{Binding .}" />
                            </Frame.GestureRecognizers>
                            
                            <Grid ColumnDefinitions="50,*,Auto" ColumnSpacing="15">
                                
                                <!-- Avatar -->
                                <Frame Grid.Column="0" 
                                       BackgroundColor="{StaticResource Primary}" 
                                       CornerRadius="25" 
                                       HeightRequest="50" 
                                       WidthRequest="50"
                                       Padding="0"
                                       HasShadow="False">
                                    <Label Text="{Binding FullName, Converter={StaticResource FirstCharConverter}}" 
                                           FontSize="20" 
                                           FontAttributes="Bold"
                                           TextColor="White"
                                           HorizontalOptions="Center"
                                           VerticalOptions="Center"/>
                                </Frame>
                                
                                <!-- Info -->
                                <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="3">
                                    <Label Text="{Binding FullName}" 
                                           FontSize="16" 
                                           FontAttributes="Bold"
                                           TextColor="{StaticResource Gray900}"/>
                                    <Frame BackgroundColor="{Binding IsActive, Converter={StaticResource ActiveStatusColorConverter}}"
                                           CornerRadius="8"
                                           Padding="8,2"
                                           HasShadow="False"
                                           HorizontalOptions="Start">
                                        <Label Text="{Binding IsActive, Converter={StaticResource ActiveStatusTextConverter}}" 
                                               FontSize="10" 
                                               TextColor="White"/>
                                    </Frame>
                                </VerticalStackLayout>
                                
                                <!-- Edit Arrow -->
                                <Label Grid.Column="2" 
                                       Text="›" 
                                       FontSize="24" 
                                       TextColor="{StaticResource Gray400}"
                                       VerticalOptions="Center"/>
                                
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
                
                <CollectionView.EmptyView>
                    <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="10">
                        <Label Text="👥" FontSize="48" HorizontalOptions="Center"/>
                        <Label Text="Henüz çalışan yok" 
                               FontSize="16"
                               TextColor="{StaticResource Gray500}"
                               HorizontalOptions="Center"/>
                        <Label Text="+ butonuna tıklayarak ekleyin" 
                               FontSize="12"
                               TextColor="{StaticResource Gray400}"
                               HorizontalOptions="Center"/>
                    </VerticalStackLayout>
                </CollectionView.EmptyView>
            </CollectionView>
        </RefreshView>
    </Grid>
</ContentPage>
